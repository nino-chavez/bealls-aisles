# BRD Open Questions

> **Status: live.** Open items that block or shape BRD stories. Resolved items move to a "Resolved" section with the resolution + date.
>
> Add items here as they surface. Don't let them die in chat threads.

## Open

> _to be populated. Seed entries to expect:_
>
> - Q1 — How does merchant rule authoring (admin) handle conflicts when two rules target the same surface? First-write-wins, priority field, or visual conflict resolver?
> - Q2 — Is `for-you-row` content scoped per session, per-shopper-cookie, or per-logged-in-account? Affects how cold-start handles cookie-only known shoppers.
> - Q3 — Bealls has three brands with shared loyalty (Bealls Bucks). Do we model loyalty state at the family level or per-brand? Affects cart/checkout schemas.

## Resolved

> _empty — items move here with resolution + date when settled._
