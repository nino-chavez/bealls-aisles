#!/usr/bin/env node
/** Retired with capture-bealls.mjs because its generation and cost scenes are historical. */

throw new Error('Historical demo-reel recapture is retired; run npm run capture:runtime-parity');
