#!/usr/bin/env node
/**
 * Retired historical demo-reel generator.
 *
 * The archived reel depicts shopper-paid generation and other behavior the
 * current runtime rejects. This entrypoint is intentionally a permanent
 * no-call stub: it reads no credentials, performs no TTS, writes no media,
 * and cannot turn the archived captions into current publication evidence.
 *
 * Use `npm run capture:runtime-parity` for current no-paid-call regression
 * evidence. A replacement reel requires a separately reviewed script and
 * captions written against the current named-zone contracts.
 */

throw new Error('Historical demo-reel generation is retired; run npm run capture:runtime-parity');
