#!/usr/bin/env node
/**
 * Retired historical demo-reel recorder.
 *
 * The archived action sequences target superseded shopper generation,
 * streaming, refinement, and cache behavior. This entrypoint is intentionally
 * a permanent no-browser stub: it opens no page, records no media, and cannot
 * turn those scenes into current runtime evidence.
 *
 * Use `npm run capture:runtime-parity` for current route and zone evidence.
 */

throw new Error('Historical demo-reel recording is retired; run npm run capture:runtime-parity');
