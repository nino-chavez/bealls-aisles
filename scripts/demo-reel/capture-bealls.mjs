#!/usr/bin/env node
/**
 * Retired historical demo-reel capture.
 *
 * The recorded reel predates the fixed shopper composition policy. It depicts
 * shopper-paid layout generation, refinement, cache bypass, and cost telemetry
 * that the current runtime rejects. Re-capturing those scenes would create
 * false evidence, so this command fails closed.
 *
 * Use `npm run capture:runtime-parity` for current no-paid-call regression
 * evidence. A new merchant-review reel needs a separately reviewed script and
 * captions written against the current named-zone contracts.
 */

throw new Error('Historical demo-reel capture is retired; run npm run capture:runtime-parity');
